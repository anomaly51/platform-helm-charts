{{- with .Values.registryPullSecret }}
{{- if .enabled }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .name }}
  namespace: {{ include "app.namespace" $ }}
spec:
  refreshInterval: {{ .refreshInterval | quote }}
  secretStoreRef:
    name: {{ .secretStoreRef.name }}
    kind: {{ .secretStoreRef.kind }}
  target:
    name: {{ .name }}
    creationPolicy: Owner
    {{- with .deletionPolicy }}
    deletionPolicy: {{ . }}
    {{- end }}
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"{{ .registry }}":{"username":"{{ "{{ .username }}" }}","password":"{{ "{{ .password }}" }}","auth":"{{ "{{ printf \"%s:%s\" .username .password | b64enc }}" }}"}}}
  data:
    - secretKey: username
      remoteRef:
        key: {{ .remoteRef.key }}
        property: {{ .remoteRef.usernameProperty }}
    - secretKey: password
      remoteRef:
        key: {{ .remoteRef.key }}
        property: {{ .remoteRef.passwordProperty }}
{{- end }}
{{- end }}
{{- range .Values.externalSecrets }}
{{- $secretStoreRef := default dict .secretStoreRef }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .name }}
  namespace: {{ default (include "app.namespace" $) .namespace }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  refreshInterval: {{ default "5m" .refreshInterval | quote }}
  secretStoreRef:
    name: {{ default "vault-backend" $secretStoreRef.name }}
    kind: {{ default "ClusterSecretStore" $secretStoreRef.kind }}
  target:
    name: {{ default .name .targetName }}
    creationPolicy: {{ default "Owner" .creationPolicy }}
    {{- with .deletionPolicy }}
    deletionPolicy: {{ . }}
    {{- end }}
    {{- with .template }}
    template:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- with .data }}
  data:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .dataFrom }}
  dataFrom:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
