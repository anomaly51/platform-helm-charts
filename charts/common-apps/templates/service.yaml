{{- if and .Values.service.enabled .Values.image.repository }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-apps.fullname" . }}
  namespace: {{ include "common-apps.namespace" . }}
  labels:
    {{- include "common-apps.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "common-apps.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
{{- end }}
